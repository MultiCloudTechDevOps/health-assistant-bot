version: 1
backend:
  phases:
    build:
      commands:
        - cd ./ # ensure we’re in project root
        - amplifyPull --appId d36kep6f589kql --envName dev
frontend:
  phases:
    preBuild:
      commands:
        - npm install --legacy-peer-deps
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*

